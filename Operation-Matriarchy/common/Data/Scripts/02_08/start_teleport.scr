OnMessage(Code=game_start){
   FadeCamera(StartColor=0xFF000000, StopColor=0x00000000, Time=2);
   ShowHud(Vis=0);
   Delay(Time=.1);
   TeleportPlayer();
   ShowHud(Vis=1);
   SendAll(Code=mark02);
   SetObjective(On=1, Name=02_08a);
}